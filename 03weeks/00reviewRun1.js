var x=1; // 이 기호는 한줄 주석입니다.
       let y=2;
       var x=4;
       // let y=3; -- let은 중복선언이 안된다.
       const a=3.14;
       // a=7.5; a라는 변수를 상수로서 설정을 하면 중복선언x
       const PI=3.14 // 상수의 변수개념은 all 대문자로 써야함
        /*
         이 기호는 
         여려줄 주석입니다.
       */
       console.log("두 숫자의 합" + (x+y));
      // document.write('<h1>두 숫자의 합\"웹\": '+ (x+y) +  "<h1> ");